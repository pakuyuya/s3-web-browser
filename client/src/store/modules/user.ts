interface UserState {
    name: string;
}

const state: UserState = {
    name: 'Guest',
};

const actions = {

};
const mutations = {

};
export default {
    namespaced: true,
    state,
    actions,
    mutations,
};
